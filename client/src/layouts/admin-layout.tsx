import {Outlet} from "react-router-dom";

const AdminLayout = () => {
    return (
        <div>
            <div>
                AdminLayout
            </div>
            <Outlet />
        </div>
    );
};

export default AdminLayout;